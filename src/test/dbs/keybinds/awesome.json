{
    "settings": {
        "name": "awesome",
    },

    "vars": {
        "modkey": "Mod4",
    },

    "values": {
        {"section": "prefix = \"$modkey\"",
        "description": "The keybinds in here all start with \"$modkey\".",
        {
            // "$" expands variable
            /// if none found it just shows the literal string
            "$modkey + m": [
                "[min|max]imize client window."
            ],
         },
        },
    }
}
